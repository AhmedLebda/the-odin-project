<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head") %>

    <body>
        <div class="grid grid-rows-layout min-h-screen">
            <%- include('../partials/header.ejs') %>

            <main class="m-8 overflow-y-auto">
                <p
                    class="italic text-xl mb-8 pb-4 border-b-2 font-bold text-red-600 capitalize"
                >
                    If you want to become a member you need to enter the
                    password
                </p>
                <form
                    class="border p-4 flex flex-col gap-4 shadow-2xl max-w-screen-sm mx-auto my-8"
                >
                    <input
                        required
                        type="text"
                        name="member-password"
                        id="member-password"
                        class="border p-2"
                        placeholder="password"
                    />

                    <button
                        type="submit"
                        class="bg-red-600 text-lg text-white border p-2 rounded-lg w-1/2 mx-auto my-4"
                    >
                        Submit
                    </button>
                </form>
            </main>

            <!-- Footer -->
            <%- include("../partials/footer") %>
        </div>
    </body>
</html>
